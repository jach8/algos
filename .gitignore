# Virtualenv — recreate with: python3 -m venv .venv && pip install -r requirements.txt
.venv/
venv/

# Python caches
.pytest_cache/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store

# Secrets (keep .env.example)
**/.env
!**/.env.example

# Runtime logs & session tapes (regenerated each run)
algo_spy/trades.jsonl
algo_spy/breadth.jsonl
algo_spy/sessions/*_tape.jsonl
algo_spy/sessions/*_meta.json
algo_spy/*.png

# Chart output anywhere in repo
*.png
